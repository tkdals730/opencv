| 용어 | 뜻 | 한 줄 설명 |
|------|-----|-----------|
| **색공간 (Color Space)** | 색을 숫자로 표현하는 방법 | BGR은 파랑·초록·빨강 세 숫자, HSV는 색상·채도·명도 세 숫자 |
| **BGR** | Blue·Green·Red | OpenCV가 사용하는 색 순서 (일반적인 RGB와 순서가 반대) |
| **HSV** | Hue·Saturation·Value | 색상(무슨 색) · 채도(얼마나 선명) · 명도(얼마나 밝음) |
| **Hue (색상)** | 색의 종류 | OpenCV에서 0~179 범위. 0=빨강, 60=초록, 120=파랑 |
| **이진화 (Thresholding)** | 기준값으로 흑백 나누기 | 픽셀이 기준보다 밝으면 흰색(255), 어두우면 검정(0) |
| **임계값 (Threshold)** | 흑백을 나누는 기준값 | "이 밝기 이상이면 흰색" 하는 그 경계 숫자 |
| **오츠 (Otsu)** | 자동으로 최적 임계값을 찾는 알고리즘 | 히스토그램을 분석해서 가장 잘 나뉘는 값을 자동 계산 |
| **적응형 이진화 (Adaptive)** | 영역마다 다른 임계값 적용 | 그림자가 있는 이미지에서 한 개 임계값으로는 부족할 때 사용 |
| **ROI (Region of Interest)** | 관심 영역 | 이미지에서 처리하고 싶은 특정 부분만 잘라낸 것 |
| **마스크 (Mask)** | 흑백 이미지로 된 필터 | 흰색 부분만 통과시키고, 검정 부분은 차단하는 역할 |
| **트랙바 (Trackbar)** | 슬라이더 UI | 파라미터 값을 마우스로 실시간 조절할 수 있는 바 |
| **히스토그램 (Histogram)** | 밝기별 픽셀 수 그래프 | 이미지에서 어두운 픽셀이 많은지, 밝은 픽셀이 많은지 한눈에 파악 |
| **크로마키 (Chroma Key)** | 특정 색 X배경을 다른 배경으로 교체 | 초록 배경 앞에서 찍고 → 초록색 제거 → 다른 배경 합성 |
| 컨투어 (Contour) | 물체의 외곽선 | 이진 이미지에서 검정(0)과 흰색(255)의 경계 |
| findContours() | 컨투어 찾기 | 이진 이미지에서 모든 외곽선을 찾아 리스트로 반환 |
| 모멘트 (Moment) | 형상의 중심/넓이 정보 | cv2.moments()로 계산되는 기하학적 특징값 |
| 중심좌표 (Centroid) | 물체의 무게중심 | 모멘트 M10, M01에서 계산: cx = M10/M00, cy = M01/M00 |
| 바운딩박스 (Bounding Box) | 물체를 감싸는 가장 작은 직사각형 | 물체의 위치와 크기를 빠르게 파악 |
| 면적 (Area) | 컨투어 내부의 픽셀 수 | cv2.contourArea() 또는 모멘트 M00 |
| 둘레 (Perimeter) | 컨투어의 외곽선 길이 | cv2.arcLength()로 계산 |
| 계층구조 (Hierarchy) | 컨투어 간의 포함 관계 | 부모-자식 관계: RETR_EXTERNAL vs RETR_TREE |
| RETR_EXTERNAL | 가장 바깥 컨투어만 | 내부 자식 컨투어는 무시 |
| RETR_TREE | 모든 컨투어와 계층 | 전체 포함 관계를 트리로 표현 |
| ROI (Region of Interest) | 관심 영역 | 이미지에서 처리하고 싶은 특정 부분만 잘라냄 |
| 히스토그램 (Histogram) | 밝기별 픽셀 수 그래프 | 이미지의 밝기 분포를 파악 |
| 기하학 변환                 | 이미지의 형태를 기하학적으로 변형      | 평행이동, 회전, 확대축소 등         |
| 아핀 변환 (Affine)         | 3개의 점 대응으로 정의되는 변환      | 평행선이 평행으로 유지됨            |
| 원근 변환 (Perspective)    | 4개의 점 대응으로 정의되는 변환      | 4점 기준 시점 변환              |
| BEV (Bird's Eye View)  | 위에서 본 시점                | 원근 변환으로 도로를 위에서 본 것처럼 만듦 |
| 에지 검출 (Edge Detection) | 픽셀 밝기가 급격히 변하는 부분 검출    | Canny, Sobel 등           |
| 모폴로지 연산                | 픽셀 집단의 형태를 변형           | 침식, 팽창, 열기, 닫기           |
| 침식 (Erosion)           | 작은 객체 제거, 경계 축소         | 흰색 영역 줄임                 |
| 팽창 (Dilation)          | 끊어진 부분 연결, 구멍 채우기       | 흰색 영역 늘림                 |
| 열기 (Opening)           | 침식 후 팽창                 | 작은 노이즈 제거                |
| 닫기 (Closing)           | 팽창 후 침식                 | 작은 구멍 채우기                |
| Canny 에지 검출            | 2단계 임계값으로 강한 에지만 추출     | 잡음 적고 경계 검출이 정확함         |
| 필터링                    | 이웃 픽셀 값을 이용해 새로운 픽셀값 계산 | 가우시안, 미디언, 바이레터럴 등       |
| 템플릿 매칭 | Template Matching | 템플릿 이미지를 큰 이미지 내에서 슬라이딩하며 유사도 계산 → 오브젝트 위치 탐지 |
| 매칭 방법 | Matching Method | 유사도 계산 방식: TM_SQDIFF, TM_CCORR, TM_CCOEFF 등 6가지 |
| 정규화 매칭 | Normalized Matching | TM_CCOEFF_NORMED → 조명 변화에 강한 정규화 버전 |
| 허프 변환 | Hough Transform | 이미지의 직선/원 같은 기하학적 형태를 자동으로 검출 |
| 극좌표 | Polar Coordinates | (ρ, θ) → 거리와 각도로 표현하는 좌표계 |
| 누산기 | Accumulator | 허프 변환에서 각 기하학적 형태의 "투표" 카운트 |
| 임계값 | Threshold | 누산기 값이 이 이상이어야 직선/원으로 인정 |
| 허프 직선 | Hough Lines | cv.HoughLines() → 극좌표 (ρ, θ)로 표현된 무한 직선 |
| 허프 선분 | Hough Lines P | cv.HoughLinesP() → 시작/끝 점 (x1, y1, x2, y2)로 표현 (더 실용적) |
| 허프 원 | Hough Circles | cv.HoughCircles() → 원의 중심 (x, y)과 반지름 r 검출 |
| 차선 인식 파이프라인 | Lane Detection Pipeline | 카메라 입력 → 전처리 → 에지 검출 → 허프 변환 → 차선 표시 |